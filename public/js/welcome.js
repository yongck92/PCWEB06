$(document).ready(() => {
    alert('Welcome user!')

    $(".h-16")
        .fadeIn({duration: 500})
        .css("display","none")
        .slideDown(500);

        $(".ml-12")
        .fadeIn({duration: 1000})
        .css("display","none")
        .slideDown(1000);

});